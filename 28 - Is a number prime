function isPrime(num) {
  let root = Math.sqrt(num);
  if (num < 2) {
     return false;
  }else if (num === 2) {
     return true;
  }

  for (let i = 2; i <= root; i++) {
     if (num % i === 0) {
       return false;
     }
  }
  return true;
}
